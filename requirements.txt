# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0

# BioPython for structure manipulation
biopython>=1.79

# OpenMM for MD simulations
# Install via conda: conda install -c conda-forge openmm
# openmm>=7.7.0

# PDBFixer for structure preparation
pdbfixer>=1.8.1

# MDTraj for trajectory analysis
mdtraj>=1.9.7

# ProDy for structure analysis
prody>=2.0

# PyYAML for config files
pyyaml>=6.0

# For docking (install via conda or separately)
# smina: conda install -c conda-forge smina
# or AutoDock Vina: https://vina.scripps.edu/

# For ligand preparation (optional)
# openbabel: conda install -c conda-forge openbabel
# rdkit: conda install -c conda-forge rdkit

# For MM/GBSA analysis (optional)
# gmx_MMPBSA or MMPBSA.py from AmberTools
